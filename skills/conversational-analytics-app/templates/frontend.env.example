# Env vars read by frontend_chat.jsx / frontend_feedback.jsx (Vite). Copy to
# `.env` in the frontend project root and fill in.

# The backend's base URL. Defaults to http://localhost:8080 if unset — only
# need to set this to point at a deployed Cloud Run backend.
VITE_BACKEND_URL=http://localhost:8080
